<% track.notes.all.each do |note| %>
  <ul>
    <li><%= note.note %> - <%= note.user.email %>
      <%= button_to 'Destroy Note', note_url(note), method: :delete, class: 'note-button' %>
    </li>
  </ul>
<% end %>